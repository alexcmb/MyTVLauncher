<resources>
    <!-- No Leanback parent anymore: the UI is Compose, which paints its own dark surface. -->
    <style name="Theme.JustTvLauncher" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
